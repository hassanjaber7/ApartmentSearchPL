This is a playwright program that searches in OLX and Otodom in order to find rooms and apartments in Poland according to the User preferences(WIP) and upon finding some listings, results are sent via telegram.
It runs automatically each couple of hours (more or less) in the GitHub actions using Cron.
The listing json file that saves all the found listings during the day are cleared automatically each midnight for storage opt.
